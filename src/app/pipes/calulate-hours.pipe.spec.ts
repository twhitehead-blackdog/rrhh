import { CalulateHoursPipe } from './calulate-hours.pipe';

describe('CalulateHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CalulateHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
